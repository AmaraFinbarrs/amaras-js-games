<!DOCTYPE html>
<html>
  <head>
    <title>AmarasJsGames</title>
    <%= favicon_link_tag asset_path('ajg-favicon.png') %>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <script src="https://kit.fontawesome.com/770b971a84.js" crossorigin="anonymous"></script>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <header><%= render 'shared/navbar'%></header>

    <main class="yield-content">
      <%= yield %>
    </main>

    <div class="footer"><%= render 'shared/footer'%><div>
  </body>
</html>
